apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: airlines-flight
  namespace: airlines-backend-microservices
spec:
  to:
    kind: Service
    name: airlines-flight
  port:
    targetPort: 8080
  tls:
    termination: edge
