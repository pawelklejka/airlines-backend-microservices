apiVersion: v1
kind: Service
metadata:
  name: mongodb-service
  namespace: airlines-backend-microservices
spec:
  clusterIP: None                      # headless, gives stable DNS per pod
  ports:
    - name: mongo
      port: 27017
      targetPort: mongo
  selector:
    app: mongodb                       # picks up the StatefulSet’s Pods
